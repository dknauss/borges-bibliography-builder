---
phase: 03-1-3-0-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/quick/4-large-bibliography-benchmark/4-PLAN.md
  - SPEC.md
  - package.json
  - bibliography-builder.php
  - readme.txt
autonomous: true
requirements:
  - bump-version-package-json
  - bump-version-plugin-header
  - bump-version-readme-stable-tag
  - write-changelog-entry
  - commit-planning-docs
  - commit-version-bump
  - tag-v1-3-0

must_haves:
  truths:
    - "All three version locations read 1.3.0: package.json, bibliography-builder.php plugin header, readme.txt Stable tag"
    - "readme.txt has a = 1.3.0 = changelog entry above = 1.2.0 ="
    - "Planning-doc updates are in a committed chore commit before the version bump commit"
    - "A version bump + changelog commit exists with message starting 'chore: prepare 1.3.0 release'"
    - "git tag lists v1.3.0"
    - "All existing tests pass before tagging"
  artifacts:
    - path: "package.json"
      provides: "npm package version"
      contains: '"version": "1.3.0"'
    - path: "bibliography-builder.php"
      provides: "WordPress plugin header version"
      contains: "Version:           1.3.0"
    - path: "readme.txt"
      provides: "WordPress.org stable tag and changelog"
      contains: "Stable tag: 1.3.0"
  key_links:
    - from: "readme.txt"
      to: "== Changelog =="
      via: "= 1.3.0 = section above = 1.2.0 ="
      pattern: "= 1\\.3\\.0 ="
    - from: "git tag v1.3.0"
      to: ".github/workflows/wp-deploy.yml"
      via: "release: types: [published] trigger — tag alone is NOT enough; user must publish a GitHub Release"
      pattern: "v1\\.3\\.0"
---

<objective>
Commit the pending planning-doc updates, bump the plugin version to 1.3.0 in all three
required locations, write the changelog entry, run the full test suite and build, then tag
v1.3.0 so the user can publish a GitHub Release to trigger the WordPress.org SVN deploy.

Purpose: Ship Phase 2 stabilization work (50-citation cap, async stale guards, manual-entry
single-format path, smaller release zip, PMID/DOI hardening) to WordPress.org users.

Output: Two commits on the current branch plus a v1.3.0 tag.
</objective>

<execution_context>
@/Users/danknauss/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danknauss/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<!-- Key facts extracted so executor needs no codebase exploration -->
<!-- package.json line 3: "version": "1.2.0" -->
<!-- bibliography-builder.php line 5: Version:           1.2.0 -->
<!-- readme.txt line 7: Stable tag: 1.2.0 -->
<!-- readme.txt line 135: == Changelog == -->
<!-- readme.txt line 137: = 1.2.0 = (first changelog entry) -->
<!-- release zip is always named borges-bibliography-builder.zip regardless of version -->
<!-- wp-deploy.yml triggers on: release types: [published] — tag alone does NOT deploy -->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit pending planning-doc updates</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    .planning/quick/4-large-bibliography-benchmark/4-PLAN.md
    SPEC.md
  </files>
  <action>
    Stage and commit the five modified/new planning files that are already in the working tree
    (confirmed via git status: .planning/STATE.md, .planning/ROADMAP.md, .planning/PROJECT.md,
    SPEC.md are modified; .planning/quick/4-large-bibliography-benchmark/ is untracked).

    Commit message (exact):
    ```
    chore: update planning docs and add Phase 3/4 to roadmap

    - Mark Phase 2 status as committed in STATE.md
    - Add Phase 3 (1.3.0 release prep) and Phase 4 (Cite/Export) to ROADMAP.md
    - Update current focus in PROJECT.md
    - Add 4-PLAN.md benchmark quick task
    - Extend 50-citation cap note in SPEC.md to keep options 2/3 open

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    ```

    Stage only these specific files — do NOT use git add -A.
    Verify with `git status` after commit that working tree shows only untracked
    non-planning files (if any).
  </action>
  <verify>
    git log --oneline -1 | grep "update planning docs"
    git show --name-only HEAD | grep "STATE.md"
  </verify>
  <done>
    `git log --oneline -1` shows the planning-docs chore commit.
    `git show --name-only HEAD` includes STATE.md, ROADMAP.md, PROJECT.md, SPEC.md,
    and the 4-PLAN.md file.
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version and write 1.3.0 changelog entry</name>
  <files>
    package.json
    bibliography-builder.php
    readme.txt
  </files>
  <action>
    Make three precise edits, then commit.

    **1. package.json** — change line 3 from:
    ```
    	"version": "1.2.0",
    ```
    to:
    ```
    	"version": "1.3.0",
    ```

    **2. bibliography-builder.php** — change line 5 from:
    ```
     * Version:           1.2.0
    ```
    to:
    ```
     * Version:           1.3.0
    ```

    **3. readme.txt**
    Change line 7 (Stable tag) from `Stable tag: 1.2.0` to `Stable tag: 1.3.0`.

    Then insert the following changelog entry between `== Changelog ==` (line 135)
    and `= 1.2.0 =` (line 137). Insert a blank line after the `== Changelog ==`
    heading, then the new entry, then a blank line before `= 1.2.0 =`:

    ```
    = 1.3.0 =
    * Enforce an explicit 50-citation total cap per bibliography block with inline editor warnings, replacing the silent 51-entry formatter cliff in 1.x.
    * Guard all async editor mutation flows (paste/import, manual add, delete, style switch, structured edit) against stale results from concurrent in-flight format requests.
    * Format the merged bibliography once in the manual-entry add path; the previous double-format on add is removed.
    * Prune non-runtime vendor documentation and images from the release zip and exclude composer.lock, reducing release package weight.
    * Cache successful PMID proxy responses and deduplicate DOI resolution requests to reduce avoidable network traffic.
    * Refactor editor code into focused hooks: useCitationImportActions, useManualCitationActions, and useBibliographyExportActions.
    * Extract PHP PMID resolver, cache, and permission logic into includes/pmid.php.
    ```

    After the three file edits, run the full test suite and build to confirm nothing
    regressed before committing:

    ```bash
    npm test -- --runInBand
    composer test:php
    npm run build
    ```

    If any test suite fails, stop and report — do NOT commit with failing tests.

    Commit only after all checks pass. Commit message (exact):
    ```
    chore: prepare 1.3.0 release

    Bump version to 1.3.0 in package.json, bibliography-builder.php plugin
    header, and readme.txt Stable tag. Add 1.3.0 changelog entry covering:
    explicit 50-citation policy, async stale-result guards, manual-entry
    single-format path, smaller release zip, PMID/DOI network hardening, and
    editor hook / PHP maintainability splits.

    Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
    ```

    Stage only package.json, bibliography-builder.php, and readme.txt.
  </action>
  <verify>
    <automated>grep -c "1\.3\.0" package.json bibliography-builder.php readme.txt</automated>
  </verify>
  <done>
    `grep "1\.3\.0" package.json bibliography-builder.php readme.txt` prints at least one
    match in each file.
    `grep "= 1\.3\.0 =" readme.txt` returns a match.
    `npm test -- --runInBand` exits 0.
    `composer test:php` exits 0.
    `npm run build` exits 0.
    `git log --oneline -2` shows this commit immediately after the planning-docs commit.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify release artifact and tag v1.3.0</name>
  <files></files>
  <action>
    Run the release package script to confirm the zip builds cleanly and contains
    the updated plugin header:

    ```bash
    npm run package:release
    ```

    Then confirm the version inside the zip matches 1.3.0:

    ```bash
    unzip -p borges-bibliography-builder.zip \
      borges-bibliography-builder/bibliography-builder.php \
      | grep "Version:"
    ```

    If the header shows `Version:           1.3.0`, proceed to tag.

    Create the annotated tag:

    ```bash
    git tag -a v1.3.0 -m "Release 1.3.0

    Explicit 50-citation total cap, async stale-result guards, manual-entry
    single-format path, smaller release zip, and PMID/DOI network hardening."
    ```

    Verify the tag exists locally:

    ```bash
    git tag | grep v1.3.0
    ```

    IMPORTANT — the tag alone does NOT deploy to WordPress.org. The GitHub Actions
    workflow (wp-deploy.yml) triggers on `release: types: [published]`. After this
    plan completes, the user must:
    1. Push the branch and tag to the remote: `git push origin HEAD && git push origin v1.3.0`
    2. Go to https://github.com/dknauss/borges-bibliography-builder/releases/new
    3. Select the v1.3.0 tag, set the release title, and click "Publish release"
    4. The wp-deploy.yml workflow will then auto-deploy to WordPress.org SVN.

    Do NOT push automatically — the user must confirm before any push.
  </action>
  <verify>
    <automated>git tag | grep v1.3.0</automated>
  </verify>
  <done>
    `git tag | grep v1.3.0` returns `v1.3.0`.
    `unzip -p borges-bibliography-builder.zip borges-bibliography-builder/bibliography-builder.php | grep "Version:"` shows `Version:           1.3.0`.
    `npm run package:release` completed without error.
    User has been informed of the manual publish-release step required to trigger WordPress.org deploy.
  </done>
</task>

</tasks>

<verification>
Run after all three tasks complete:

```bash
# All three version locations must match
grep "1\.3\.0" package.json bibliography-builder.php readme.txt

# Changelog entry exists
grep "= 1\.3\.0 =" readme.txt

# Tag exists
git tag | grep v1.3.0

# Two new commits (planning docs + version bump)
git log --oneline -3

# Tests still pass
npm test -- --runInBand
```
</verification>

<success_criteria>
- `grep "1\.3\.0" package.json` matches `"version": "1.3.0"`
- `grep "1\.3\.0" bibliography-builder.php` matches `Version:           1.3.0`
- `grep "1\.3\.0" readme.txt` matches both `Stable tag: 1.3.0` and `= 1.3.0 =`
- `git tag | grep v1.3.0` returns `v1.3.0`
- `npm test -- --runInBand` exits 0
- `composer test:php` exits 0
- `npm run build` exits 0
- `npm run package:release` produces borges-bibliography-builder.zip containing `Version: 1.3.0` in the plugin header
- Two new commits exist: the planning-docs chore and the version bump chore
- User knows they must push and publish the GitHub Release to trigger WordPress.org deploy
</success_criteria>

<output>
After completion, create `.planning/archive/phases/03-1-3-0-release-prep/03-01-SUMMARY.md`
using the summary template at `@/Users/danknauss/.claude/get-shit-done/templates/summary.md`.
</output>
